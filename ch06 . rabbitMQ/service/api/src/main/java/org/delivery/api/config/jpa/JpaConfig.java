package org.delivery.api.config.jpa;

import org.springframework.boot.autoconfigure.domain.EntityScan;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.jpa.repository.config.EnableJpaRepositories;
/*
@Configuration // 환경 설정 클래스로 선언
@EntityScan(basePackages = "org.delivery.db") // JPA 엔티티가 위치한 패키지를 지정
@EnableJpaRepositories(basePackages = "org.delivery.db") // JPA Repository 가 위치한 패키지를 지정
public class JpaConfig {
}*/
